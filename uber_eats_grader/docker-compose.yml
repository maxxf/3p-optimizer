version: '3'
services:
  uber-eats-grader:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
